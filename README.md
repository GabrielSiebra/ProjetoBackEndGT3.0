Como rodar o programa:
1 - npm install
2 - Configurar o arquivo .env com as credenciais do MySQL.
3 - npx sequelize-cli db:migrate (para criar as tabelas).
4 - npm test (para provar que a segurança está funcionando).
5 - npm run dev (para iniciar o servidor).
